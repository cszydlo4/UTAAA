@model IEnumerable<UTAAA.Models.AccessModel>

@{
    Layout = "~/Views/Shared/HideTabLayout.cshtml";
}

<h3 id="center">Select security access:</h3>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.SECURITYACCESSDESC)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.SECURITYACCESSDESC)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>
